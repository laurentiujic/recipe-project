spring:
  datasource:
    url: jdbc:mysql://localhost:3306/laur_prod
    username: laur_prod_user
    password: laur
  jpa:
    hibernate:
      ddl-auto: validate
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    database: mysql
    show-sql: false